import com.jackwharton.mosaic.runMosaic
import androidx.compose.runtime.*
import androidx.compose.foundation.layout.*
import androidx.compose.material.Text
import androidx.compose.material.Box

class kw_moz {
	fun newbutton(String text_button) { Box { Text(text_button) } }
	fun newlabel(String text_label) { Text(text_label) }
}
